$('#comments-list').html("<%= j (render partial: 'post_comments/comments', locals: { post: @post }) %>");
$('#comment-form').html("<%= j (render partial: 'post_comments/new_comment_form', locals: { post: @post }) %>");