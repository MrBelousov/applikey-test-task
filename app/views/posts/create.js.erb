$('#posts-list').html("<%= j (render 'posts/posts') %>");
$('#post-form').html("<%= j (render 'posts/new_post_form') %>");
<% user = current_user %>
$('#posts-count').html("Posts (<%= user.posts.count %>)");

